version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Install dependencies with npm
        - npm ci
    build:
      commands:
        # Build Vite project
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
